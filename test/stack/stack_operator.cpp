
#include "stack.hpp"
#include <iostream>
#include <stack>
#include "utils.hpp"

#ifndef NAMESPACE
# define NAMESPACE ft
#endif

int main(void)
{
    std::cout << "stack constructor test" << std::endl;
   /*
    NAMESPACE::stack<int> s;
    NAMESPACE::stack<int> s2(s);

    for (int i = 0; i < 10; i++)
        s.push(i);
    for (int i = 10; i > 0; i--)
        s2.push(i);
    std::cout << "s: " << s.size() << std::endl;
    std::cout << "s2: " << s2.size() << std::endl;
    // test operator relational
    std::cout << "s == s2: " << (s == s2) << std::endl;
    std::cout << "s != s2: " << (s != s2) << std::endl;
    std::cout << "s < s2: " << (s < s2) << std::endl;
    std::cout << "s <= s2: " << (s <= s2) << std::endl;
    std::cout << "s > s2: " << (s > s2) << std::endl;
    std::cout << "s >= s2: " << (s >= s2) << std::endl;
    // test operator relational
    std::cout << "s == s: " << (s == s) << std::endl;
    std::cout << "s != s: " << (s != s) << std::endl;
    std::cout << "s < s: " << (s < s) << std::endl;
    std::cout << "s <= s: " << (s <= s) << std::endl;
    std::cout << "s > s: " << (s > s) << std::endl;
    std::cout << "s >= s: " << (s >= s) << std::endl;
    // test operator relational
    s = s2;
    std::cout << "s == s2: " << (s == s2) << std::endl;
    std::cout << "s != s2: " << (s != s2) << std::endl;
    std::cout << "s < s2: " << (s < s2) << std::endl;
    std::cout << "s <= s2: " << (s <= s2) << std::endl;
    std::cout << "s > s2: " << (s > s2) << std::endl;
    std::cout << "s >= s2: " << (s >= s2) << std::endl;
    */
    return 0;
}